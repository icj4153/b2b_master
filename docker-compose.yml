version: "3.8"

services:
  b2b-excel:
    build: .
    container_name: b2b-excel-crawler
    restart: unless-stopped
    environment:
      TZ: KST-9
      B2B_RUN_AT: "09:00"
      B2B_RUN_ON_START: "false"
      B2B_DOWNLOAD_DIR: /app/b2b_downloads
      B2B_OUTPUT_DIR: /app/output
    env_file:
      - .env
    volumes:
      - ./b2b_list:/app/b2b_list:ro
      - ./b2b_downloads:/app/b2b_downloads
      - ./output:/app/output
